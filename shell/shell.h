#ifndef SHELL_H
#define SHELL_H

/* Initialize the shell (displays welcome banner) */
void shell_init(void);

/* Enter the infinite shell loop (prompts, reads user input, executes commands) */
void shell_loop(void);

#endif /* SHELL_H */
